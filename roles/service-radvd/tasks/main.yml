---
- name: install radvd packages
  apt:
    name: radvd
    state: present

- name: enable systemd unit radvd
  systemd:
    name: radvd
    enabled: yes
    daemon_reload: yes

- name: configure radvd
  template:
    src: radvd.conf.j2
    dest: /etc/radvd.conf
    #notify: restart radvd
