[Unit]
Description=BIRD Prometheus Exporter
Wants=network.target
After=network.target

[Service]
Type=simple
User=root
Group=root
ExecStart={{ bird_exporter_path }} {{ bird_exporter_default_opts }}
Restart=always
RestartSec=5s

[Install]
WantedBy=multi-user.target
