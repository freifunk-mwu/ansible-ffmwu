[Unit]
Description=Collect checkrestart metrics for prometheus-node-exporter

[Service]
Type=oneshot
Environment=TMPDIR={{ prometheus_textfile_path }}
ExecStart=/bin/bash -c "/usr/local/sbin/checkrestart_exporter.sh | sponge {{ prometheus_textfile_path }}/checkrestart.prom"
